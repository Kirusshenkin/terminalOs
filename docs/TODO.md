# TODO — сессия 24.09.2026

Живой список: что запущено, что найдено, что осталось. Обновляется после
каждого шага агентов. Решения — только после «добро» Кирилла (см. план).

## Блокеры
- [~] Лицензия Xcode не принята. Обход для сборки и тестов: CommandLineTools +
      заглушка `metal` (рендер Metal из SwiftTerm приложением не используется) +
      плагины макросов SwiftUI из Xcode (scratchpad/swiftenv.sh). Для **релизной
      сборки и деплоя в /Applications** лицензию всё равно надо принять:
      `! sudo xcodebuild -license accept`

## Агенты
| # | Задача | Статус | Результат |
|---|--------|--------|-----------|
| A1 | Ресерч конкурентов: плюсы/минусы, горячие клавиши, AI | готово | docs/research/competitors.md |
| A2 | Аудит UI/UX: где шумно, что спрятано, путь новичка | готово (только по коду) | docs/research/uiux-audit.md |
| A3 | Горячие клавиши: что есть, предложение раскладки | готово | docs/research/hotkeys.md |
| A4 | AI ↔ терминал: что уже есть, почему не видно, варианты | готово | docs/research/ai-terminal.md |
| A5 | Полное тестирование (живое приложение build 20 + MCP) | брак: снимал весь экран (Telegram, VS Code), выводы про GUI недостоверны; снимки удалены | MCP: 13 инструментов работают |
| W1 | Горячие клавиши по §28 | агент сделал скрытыми кнопками вместо меню, ⇧⌘O не то, ⌘K нет → сделал сам: main f61f5db, все клавиши §28 в строке меню | — |
| W4 | Вынести захардкоженные строки интерфейса в таблицу | запущен | ветка work/strings, ../terminal-wt-strings | ветка work/hotkeys, ../terminal-wt-hotkeys |
| W2 | Навигация, направление A | влито в main 45d21f7 (агент сдал несобирающийся код и заглушки «coming soon» — переделал сам) | — |
| W3 | ИИ: #3, #7, #8 + «подключить Claude Code» | влито в main f92e036 (#3 агент починил наполовину — дочинил: режимы грузятся при входе, группа наследуется; проверка ~/.claude.json ушла с главного потока, --scope user) | — |

## Найдено (заполняется)
- A3: в приложении одна клавиша — ⌘1–9 (разделы, RootView). Нет ⌘T/⌘D/⌘W/⌘K/⌘,/⌘F.
- A3, требует правки перед показом: ⌘Z под «zoom» (это Undo), ⌘S под «download»,
  ⌘←/→ для фокуса панели (в шелле это начало/конец строки) — заменить на ⌥⌘стрелки.
- A4: MCP-мост живой (13 инструментов), но phosphor не прописан в конфиге Claude Code,
  а в приложении нет входа «подключить AI» → функции не видно. Мешают #3 (режим доступа
  не сохраняется), #7 (нет прокрутки), #8 (list_hosts без статуса).
- A1: 11 конкурентов с плюсами/минусами и источниками. Позиция Phosphor: терминал для
  AI-агентов (MCP + tmux со статусами + устойчивый SSH), нативный, без облака и подписки.
  Брать: сплит мышью, глобальная клавиша вызова, SFTP-панель, «работает без настройки».
- A2: скриншоты агента сняли VS Code, а не Phosphor (удалены, в кадр попали чужие
  данные). Выводы — по коду, живьём НЕ подтверждены → проверить в A5.
  Главное: подстраницы (ключи, проброс, сниппеты, графики, тома) спрятаны; 4 кнопки
  импорта без порядка; пустые экраны без подсказки; хост выбирается только во вкладке
  «Хосты»; «AI ACTIVITY» непонятно зачем.

## Живой осмотр build 20 (снимок только окна Phosphor, без нажатий — в окне идёт работа)
- Смешение языков: вкладки HOSTS/TERMINAL/… и «no tmux on this Mac» по-английски,
  рядом «НАПРЯМУЮ» по-русски → проверить, какие строки мимо таблицы/не переведены.
- Шапка справа «хост · НАПРЯМУЮ · TOUCH ID» переносится на две строки. → отдано W2
- Питомец (кот) и кнопки CAT/GLIDER перекрывают строки вывода терминала. → отдано W2
- Причина смешения языков найдена: ~225 русских строк захардкожены мимо таблицы
  (Host.swift `Reach.summary`/`GuardLevel.title`, DockerKit ContainerAction/Resources,
  Welcome.swift, ошибки в SessionKit/AuthKit/KeysKit…). Нарушение CLAUDE.md «ни одной
  захардкоженной строки». → W4 после слияния W2/W3 (чтобы не конфликтовать).
  Тексты MCP (ToolRunner/ToolCatalog) — для ИИ, не интерфейс: решить отдельно.
- На Маке нет tmux: локальная сессия «plain shell», рейл показывает «main idle» — ок,
  но подсказка «brew install tmux» — единственный путь, кнопки нет.
- Кирилл запускает Claude Code прямо в Phosphor → агент в сессии, а приложение
  этого не видит (довод за «агенты в рейле со статусом», §28 следующий шаг).
- Правило для тестирования: снимать только `screencapture -l <id окна>`
  (scratchpad/bin/phosphor-winid), не слать клавиши в окно, пока там работа.

## Решено (24.09, «делай сам») → PLAN.md §28
- Навигация: направление A. ИИ: подключение в один клик + #3/#7/#8; агенты в
  рейле и очередь одобрений — следующим шагом. Клавиши — раскладка §28.

## Ждёт решения Кирилла
- AI: варианты 1) подключение Claude Code в один клик, 3) агенты в рейле сессий,
  4) очередь одобрений для записи; 2) AI-строка в терминале агент не советует.

## Сделано
- Юнит-тесты на main (151b518): 244/244 зелёные.
- Ресерч и решения закоммичены: 3b089e4 (переписан: в отчёт UX попало реальное имя хоста; не был запушен).
- main после слияния W2+W3: сборка ок, 249/249 тестов.
- Найдено попутно: `claude mcp add` без `--scope user` подключает мост только к одной папке — исправлено в docs/MCP.md и в приложении.

## Осталось после агентов
- [ ] Живая проверка сборки 21 (клавиши в меню, ⇧⌘O, ⌘K, ⌘F, выбор хоста в шапке,
      кот в рейле, «доступ ИИ» + статус Claude Code, режимы MCP после перезапуска)
      — снимки только окна, клавиши не слать, пока Кирилл работает в окне
- [~] W4: вынести захардкоженные строки интерфейса в таблицу (ru+en) — агент работает
- [x] Слить work/nav, work/ai в main
- [x] Горячие клавиши (сам) — f61f5db, 250/250 тестов
- [ ] Полный прогон check.sh на слитом main
- [ ] Повторный живой тест уже новой сборки
- [ ] Сборка 21 и деплой в /Applications (нужна лицензия Xcode)
- [x] GUIDE.ru.md под новые клавиши/навигацию/ИИ
- [ ] GUIDE.en.md (план §26 требует обе версии; английской ещё нет вообще)
- [ ] Что не починили — в issues
